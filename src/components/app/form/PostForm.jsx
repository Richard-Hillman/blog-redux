import React from 'react';
import PropTypes from 'prop-types';

const PostForm = () => {
    return {
        <form onSUbmit={}>
        <input 
          type="text"
          placeholder="name"
          value={}
        onChange={} />
        <button>Create Post</button>
        </form>
    }
}